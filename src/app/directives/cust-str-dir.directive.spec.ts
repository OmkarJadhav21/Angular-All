import { CustStrDirDirective } from './cust-str-dir.directive';

describe('CustStrDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CustStrDirDirective();
    expect(directive).toBeTruthy();
  });
});
