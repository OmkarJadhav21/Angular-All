import { AsciiPipePipe } from './ascii-pipe.pipe';

describe('AsciiPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AsciiPipePipe();
    expect(pipe).toBeTruthy();
  });
});
